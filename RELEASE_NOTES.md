# 未リリース

## 機能追加

- 選択中のノードを Ctrl+C / Ctrl+V、上部ボタン、右クリックメニューからコピー・貼り付けできるようにしました。選択ノード同士の接続と、割り当て済みのタグ・登場人物・グループも引き継がれます。
- キャンバスの表示範囲外に要素がある場合、縦横のスクロールバーを自動表示するようにしました。負の座標やズームにも対応しています。
- 空白部分のドラッグをパン、Shift+ドラッグを範囲選択へ変更しました。ホイールで縦スクロール、Shift+ホイールで横スクロールできます。
- Ctrl++ / Ctrl+-、上部ボタン、右クリックメニューから拡大・縮小できるようにしました。
- ノードの接続開始を明示的なハンドルに限定し、ノードを選択するときの誤操作を減らしました。

## 修正内容

- 空白キャンバスの右クリックメニューを開いたときにアプリが終了する問題を修正しました。

# Plotree v1.0.3

2026-08-17

## 修正内容

- Microsoft Store からインストールした環境で起動できない場合がある問題を修正しました。
- Windows の表示言語が英語の場合でも、設定から日本語を選択できるようにしました。必要な日本語リソースは Microsoft Store から取得され、次回起動時に適用されます。
- 言語を「システムの既定」に戻したとき、以前に選択した言語設定が残らないようにしました。

## ダウンロード

`Plotree_1.0.3.0_x64_ARM64.msixbundle` には x64 と ARM64 のパッケージが含まれています。
